/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
        domains: ['the-mansion-of-lobellia.s3.us-east-1.amazonaws.com'],
    },
};

export default nextConfig;
